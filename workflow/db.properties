{% set workflow = settings.get('workflow', {}) -%}
db={{ domain|replace('.', '_') }}
jdbc.driver=org.postgresql.Driver
jdbc.url=jdbc:postgresql://localhost:5432/{{ domain|replace('.', '_') }}
jdbc.username={{ workflow['user'] }}
jdbc.password={{ workflow['pass'] }}
